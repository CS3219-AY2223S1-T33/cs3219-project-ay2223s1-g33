version: '3.8'
services:
  frontend:
    build:
      context: .
      dockerfile: frontend.dockerfile

    restart: always

    ports:
      - "80:80"

    expose:
      - 3000

    volumes:
      - /etc/nginx/

#  nginx:
#    build: ./nginx
#    restart: always
#
#    ports:
#      - "80:80"